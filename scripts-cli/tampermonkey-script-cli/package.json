{
  "name": "tampermonkey-script-cli",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "pnpm run format && vue-tsc",
    "dev": "vue-tsc --noEmit && vite --force",
    "format": "oxfmt . --write",
    "format:dist": "oxfmt dist --write",
    "build": "pnpm run format && vue-tsc --noEmit && vite build && pnpm run format:dist",
    "build:min": "pnpm run format && vue-tsc --noEmit && vite build --minify && pnpm run format:dist",
    "build:all": "pnpm run format && vue-tsc --noEmit && vite build && vite build --minify --no-empty-outDir && pnpm run format:dist",
    "build:all-new": "pnpm run clear:version && pnpm run build:all",
    "build:no-cache": "pnpm run format && pnpm store prune && vue-tsc --noEmit && vite build && pnpm run format:dist",
    "build:watch": "pnpm run format && vue-tsc --noEmit && vite build --watch",
    "preview": "pnpm run format && vite preview --host 0.0.0.0",
    "clear": "pnpm run clear:version && del .\\vite.config.ts.timestamp* && pnpm store prune",
    "clear:version": "del .\\SCRIPT_VERSION.json"
  }
}
