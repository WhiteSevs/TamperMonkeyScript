import { SCRIPT_NAME } from "@components/env.base";
const _SCRIPT_NAME_ = SCRIPT_NAME || "m3u8内容过滤器";

export { utils, DOMUtils, log, MenuRegister, httpx, addStyle, pops, $, $$, cookieManager } from "@components/env.base";

export { _SCRIPT_NAME_ as SCRIPT_NAME };
